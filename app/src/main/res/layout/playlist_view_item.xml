<?xml version="1.0" encoding="utf-8"?>

<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view_parent"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="5dp"
    android:paddingHorizontal="9dp"
    android:background="@android:color/transparent">

    <androidx.cardview.widget.CardView
        android:id="@+id/playlist_item_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardElevation="0dp">

            <TextView
                android:id="@+id/playlist_title_textview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/CardViewStyle"
                android:padding="@dimen/card_view_padding"
                android:textSize="@dimen/text_size"
                android:textStyle="bold"
                android:maxLines="2"
                android:gravity="center_vertical"
                android:layout_marginVertical="@dimen/card_view_text_margin"
                android:textAppearance="?attr/textAppearanceSubtitle2"
                app:drawableLeftCompat="@drawable/ic_round_music_note_64_light" />

    </androidx.cardview.widget.CardView>

</FrameLayout>